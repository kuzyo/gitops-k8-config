apiVersion: v1
kind: Service
metadata:
  name: gitops-fullstack-app-service
spec:
  selector:
    app: gitops-fullstack-app
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
  type: {{ .Values.service.type }}
